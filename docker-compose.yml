version: '3.8'

services:
  # Redis (required for BullMQ job queues)
  redis:
    image: redis:7-alpine
    container_name: voiceforge-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - voiceforge-network

  # Backend API Server
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: voiceforge-api
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=production
      - REDIS_URL=redis://redis:6379
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - voiceforge-network
    # Production resource limits (uncomment and adjust based on your VPS)
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '1.5'
    #       memory: 1G

  # Background Worker (BullMQ - handles long TTS + Voice Cloning)
  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: voiceforge-worker
    restart: unless-stopped
    command: npm run worker
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=production
      - REDIS_URL=redis://redis:6379
    depends_on:
      redis:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - voiceforge-network
    # Production resource limits - TTS generation can be CPU heavy
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '2'
    #       memory: 2G

  # Frontend (Next.js) - Uncomment when ready
  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   container_name: voiceforge-frontend
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   env_file:
  #     - ./frontend/.env.local
  #   depends_on:
  #     - api
  #   networks:
  #     - voiceforge-network

volumes:
  redis_data:
    driver: local

networks:
  voiceforge-network:
    driver: bridge
